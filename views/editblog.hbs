<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{> header}}
    <link rel="stylesheet" href="/styles.css">
    <title>Edit Blog</title>
    <style>
        .form-container {
            max-width: 800px;
            margin: 50px auto;
        }

        .form-group {
            margin-bottom: 15px;
        }
    </style>
</head>

<body>
    {{> nav}}
    <div class="container form-container">
        <h2>Edit Blog</h2>
        <form action="/blog/edit/{{blog._id}}" method="POST">
            <input type="hidden" name="_method" value="PUT">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" class="form-control bg-dark text-white" id="title" name="title"
                    value="{{blog.title}}" required>
            </div>
            <div class="form-group">
                <label for="body">Body</label>
                <textarea class="form-control bg-dark text-white" id="body" name="body" rows="10"
                    required>{{blog.body}}</textarea>
            </div>
            <div class="form-group">
                <label for="coverImageURL">Cover Image URL</label>
                <input type="text" class="form-control bg-dark text-white" id="coverImageURL" name="coverImageURL"
                    value="{{blog.coverImageURL}}" required>
            </div>
            <button type="submit" class="btn btn-primary">Update Blog</button>
        </form>
    </div>
    {{> script}}
</body>

</html>